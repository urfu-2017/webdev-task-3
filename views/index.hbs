<!DOCTYPE html>
<html lang={{ lang }}>
<head>
    <meta charset="{{ meta.charset }}">
    <title>{{ title }}</title>
    <link rel="stylesheet" type="text/css" href="/css/index.css">
</head>
<body>
    <main class="main">
        {{>search}}
        {{>place-creator}}
        {{>places}}
        {{>footer}}
    </main>
</body>
</html>
